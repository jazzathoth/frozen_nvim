-- Optional interface features. Set a value to false to disable that complete
-- feature without removing its implementation or key definitions.
return {
  buffer_menus = true,
  named_tabs = true,
  explorer_open_actions = true,
  modified_buffer_status = true,
  pane_labels = true,
}
